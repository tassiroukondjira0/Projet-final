// Doit rester strictement identique à `PRICING` défini dans le frontend
// (src/lib/api.ts) pour que les montants affichés et facturés correspondent.
const PRICING = {
  session: 2000,
  monthly: 45000,
  annual: 520000,
};

module.exports = PRICING;
